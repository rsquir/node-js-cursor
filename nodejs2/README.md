# nodejs

A small Node.js HTTP server that reads from a MySQL `data` table.

## Setup

Copy `.env.example` to `.env` and set `MYSQL_PASSWORD`.

## Run

```bash
npm start
```

Then open http://localhost:3000

For auto-reload while you edit:

```bash
npm run dev
```
